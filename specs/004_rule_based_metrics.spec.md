# Spec 004 — Deterministic metrics: exact match, regex match, JSON schema validation, numeric tolerance

## Objective

Deterministic metrics: exact match, regex match, JSON schema validation, numeric tolerance.

This spec is locked and intentionally incomplete. Do not implement until it is unlocked and expanded.

## Current Rule

Do not implement this spec yet.
