# Spec 009 — Import agent or LLM app traces and convert selected failures into evaluation cases

## Objective

Import agent or LLM app traces and convert selected failures into evaluation cases.

This spec is locked and intentionally incomplete. Do not implement until it is unlocked and expanded.

## Current Rule

Do not implement this spec yet.
