# Spec 008 — CLI command that runs evals and exits nonzero when regression gates fail

## Objective

CLI command that runs evals and exits nonzero when regression gates fail.

This spec is locked and intentionally incomplete. Do not implement until it is unlocked and expanded.

## Current Rule

Do not implement this spec yet.
