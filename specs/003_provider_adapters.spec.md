# Spec 003 — Typed provider adapter interface and fake provider for tests before real API integration

## Objective

Typed provider adapter interface and fake provider for tests before real API integration.

This spec is locked and intentionally incomplete. Do not implement until it is unlocked and expanded.

## Current Rule

Do not implement this spec yet.
